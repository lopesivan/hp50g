%%HP: T(0)A(D)F(.);
@ apps/lrand.hp50g - Lrand
@ Author: Ivan Lopes
@ E-mail: ivan (at) 42algoritmos (dot) com (dot) br
@ Version:

@ Exchange two positions
@ Given a list on level three and two position numbers on levels two and one, ‘ETP’ will exchange the
@ elements of the two position numbers.

@ ETP
\<<
  ROT
  DUP
  4
  DUPN
  DROP
  ROT
  GET
  PUT
  4
  ROLLD
  SWAP
  GET
  PUT
\>>
