%%HP: T(0)A(D)F(.);
@ apps/lrand.hp50g - Lrand
@ Author: Ivan Lopes
@ E-mail: ivan (at) 42algoritmos (dot) com (dot) br
@ Version:

@ Test a list for reals
@ Test a list for all elements being reals. Return 1 if true, 0 if not.

@ TLR
\<<
  0
  IFERR MOD
  THEN
    SWAP
    DROP
  ELSE
    DROP
    1
  END
\>>
