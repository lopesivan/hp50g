%%HP: T(0)A(D)F(.);
@ fcplx.hp50g - Fcplx
@ Author: Ivan Lopes
@ E-mail: ivan (at) 42algoritmos (dot) com (dot) br
@ Version:

\<<
    OBJ\-> DROP DROP
    DUP
    OBJ\-> DROP DROP
    NEG
    +
    DUP
    3 ROLLD * EVAL
    UNROT
    * EVAL
    SWAP /
\>>
